FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install ALL dependencies (including dev dependencies)
RUN npm install

# Copy source code
COPY . .

EXPOSE 5000

# Default command (can be overridden)
CMD ["npm", "run", "dev"]